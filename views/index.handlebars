<div class="jumbotron">
  <h1 class="display-4 text-center">Burgers!</h1>
  <p class="lead text-center">Add or devour a delicious burger!</p>
</div>

<div class="container">
  <div class="row">
    <h3>Burgers that have been made:</h3>
    <hr>
  </div>
  <div class="row">
    <div class="col-sm-6">
      <h4>Not eaten yet!</h4>
      <ul>
        {{#each burger}}
        {{#unless devoured}}
        {{> burger/burger-block devoured=true}}
        {{/unless}}
        {{/each}}
      </ul>
    </div>

    <div class="col-sm-6">
      <h4>Devoured!</h4>
      <ul>
        {{#each burger}}
        {{#if devoured}}
        {{> burger/burger-block devoured=false}}
        {{/if}}
        {{/each}}
      </ul>
    </div>
  </div>

{{!-- make a new burger form --}}
  <div class="row">
    <form class="create-form">

      <div class="form-group">
        <label for="burgers">Burger Name:</label>
        <input type="text" id="burger" name="name">
      </div>

      <div class="form-group">
        <label for="burgers">In what column would you like this burger to appear?</label><br>
        <input type="radio" name="devoured" value="1" checked> Devoured!<br>
        <input type="radio" name="devoured" value="0"> Not eaten yet.
      </div>

      <button type="submit">Add burger</button>
    </form>
  </div>
</div>